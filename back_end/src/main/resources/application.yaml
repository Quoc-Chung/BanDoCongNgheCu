spring:
   application:
       name: bandocongnghe

   datasource:
      url: jdbc:postgresql://localhost:5432/bandocongnghe
      username: postgres
      password: Ch@/12345678
      driver-class-name: org.postgresql.Driver

   jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
   data:
     redis:
       host: localhost
       port: 7000
       password:
       expired-time: 60  # minutes

